# Build #4 — hardening the newsletter signup after it was found by bots.
#
# NOTE ON `tool`: the tool under review is Claude Code, not Buttondown. /tools is a
# projection of this collection and is an AI-tool index; Buttondown is the SUBJECT of
# the build and belongs in `stack`, not `tool`. Build #1 also reviews Claude Code —
# deliberate: that was greenfield scaffolding, this is adversarial debugging and
# security work, which says something different about the same tool.
title: "Hardening the email signup endpoint after the bots found it"
slug: "buttondown-hardening"
category: "websites"
tool: "Claude Code"
tool_version: ""      # still unknown — see BACKLOG; never guess a published version
built_with: ["Claude Opus 5"]
stack: ["Buttondown", "Netlify Functions", "Cloudflare Turnstile", "Astro 7.1"]
runs_on_site: true    # the hardened form is the one on this site
status: draft         # DRAFT — human verify pass (PROCESS step 5) not done
summary: "The newsletter signup on this site was a public endpoint with no abuse controls, and bots found it: 33 scraped addresses in 65 hours, which meant confirmation emails landing on real strangers who never asked for them. Claude Code built the layered fix — honeypot, validation, origin check, server-verified Turnstile, failing closed — in a 2h21m session, and 12 of 13 acceptance checks pass. The caveat is the part you can't delegate: three separate times a test came back green because it wasn't running where production runs, and the tool hands you those green results without complaint."
test_score: "12/13"   # acceptance tests passing; 1 deliberately skipped (see test.md)
affiliate_url: ""
repro_pack: false     # public in-repo, like build #1 — no email gate
published: ""         # unset until verified + published
# PROCESS step 5: published, last_verified and status are ALL set only after the
# human sign-off. Pre-filling last_verified made the archive row read
# "verified jul 2026" under a draft — the exact overclaim build #1 was audited for.
# The checks were run 2026-07-30 (see test.md); that is a test date, not a verification.
last_verified: ""
pricing_as_of: ""     # no cost claim for the primary tool in this build

featured: false
meta_line: "12/13 checks · 1 self-inflicted outage"

tool_summary: "AI pair-builder in your terminal"
tool_verdict: "Built the whole layered defence — honeypot, validation, server-verified Turnstile, fail-closed — in one 2h21m session, and got the ordering of the controls right without being asked. It will also hand you a passing test that never exercised production. Checking that the test matches reality is still yours."
accessibility: "Advanced"
