# Mirrors site/src/content/builds/website.mdx frontmatter — keep the two in sync.
title: "Building Jigg.AI itself, with Claude Code and Astro"
slug: "website"
category: "websites"
tool: "Claude Code + Astro"
tool_version: "Astro 7.1"
runs_on_site: true
live_url: "https://jigg.ai"   # deployed on Netlify — custom domain, HTTPS
status: verified       # built, deployed, and live at jigg.ai; core checks pass
                       # (Lighthouse + a formal a11y audit still open — see test.md)
summary: "Claude Code took this site from an empty folder to a live, four-view build-log engine where publishing a new post is adding one markdown file. The build itself went smoothly — the one caveat is that the real time sink wasn't the code at all, it was the Git and GitHub auth setup around it."
test_score: ""         # intentionally empty — site checks aren't a scored set.
                       # Deploy passed; Lighthouse not yet run. See test.md.
affiliate_url: ""      # n/a — no affiliate relationship with Claude Code or Astro,
                       # so the build log shows a verdict with no CTA
repro_pack: true
published: "2026-07-17"
last_verified: "2026-07-17"
pricing_as_of: "2026-07-17"

# ---- display + tool-projection helpers ----
featured: true
meta_line: "one collection → four views"

tool_summary: "AI pair-builder in the terminal · Astro is free and open-source"
tool_verdict: "Empty folder to a live, four-view content engine in one sitting. The code was the easy part; the Git and GitHub auth setup around it was the real friction — none of it the tool's fault."
accessibility: "Some setup"
