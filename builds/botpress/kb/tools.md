# Tools I've actually used

Source page: https://jigg.ai/tools/

Transcribed from the rendered page — this route is generated from the builds collection,
so it changes whenever a build is added. Re-sync then.

No star ratings — just what I built with each one, how hard it was, and whether I'd reach
for it again.

The tools index only lists the **primary tool** of each build — the one thing that build
actually reviewed and tested. AI models and supporting services used along the way are
shown on the build page but never get an index entry, so this list stays a record of
things genuinely tested rather than a directory of everything touched.

## Claude Code

- Category: Websites
- **Runs on this site** — yes
- Verdict: Empty folder to a live, four-view content engine in one sitting. The code was
  the easy part; the Git and GitHub auth setup around it was the real friction — none of
  it the tool's fault.
- Summary: AI pair-builder in your terminal
- Accessibility: Some setup
- 1 build

See the build: https://jigg.ai/builds/website/
