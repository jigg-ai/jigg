---
title: "Building Jigg.AI itself, with Claude Code and Astro"
category: "websites"
tool: "Claude Code"
tool_version: "2.1.70"
built_with: ["Claude Opus 4.8", "ChatGPT 5.6"]
stack: ["Astro 7.1", "GitHub", "Netlify", "Cloudflare"]
runs_on_site: true
status: "verified"
summary: "Claude Code took this site from an empty folder to a live, four-view build-log engine where publishing a new post is adding one markdown file. The build itself went smoothly — the one caveat is that the real time sink wasn't the code at all, it was the Git and GitHub auth setup around it."
test_score: ""
repro_pack: true
published: 2026-07-17
last_verified: 2026-07-17
pricing_as_of: 2026-07-17
featured: true
meta_line: "one collection → four views"
tool_summary: "AI pair-builder in your terminal"
tool_verdict: "Empty folder to a live, four-view content engine in one sitting. The code was the easy part; the Git and GitHub auth setup around it was the real friction — none of it the tool's fault."
accessibility: "Some setup"
---

import ShortVersion from '../../components/post/ShortVersion.astro';
import ProcessStep from '../../components/post/ProcessStep.astro';
import StatTiles from '../../components/post/StatTiles.astro';
import ReproPack from '../../components/post/ReproPack.astro';
import ToolVerdict from '../../components/post/ToolVerdict.astro';

<ShortVersion>
The first build is the site you're reading. Claude Code scaffolded an Astro
project where the whole site is driven by **one content collection** — home,
archive, each build log, and the tools index all read from the same markdown
files, so adding a build is adding one file, not editing four pages. That
architecture came together cleanly. The honest caveat: the part that ate the
most time had nothing to do with building — it was untangling a chain of Git and
GitHub auth mistakes, all of them mine.
</ShortVersion>

## The honest process

The site scaffold and the four page templates were the smooth part. Getting the
repository onto GitHub was not — and every snag traced back to my own setup, not
to Claude Code or GitHub. Tagged honestly:

<ProcessStep result="fail" title="Pushed over HTTPS with a password" tag="my-setup">
The first push used HTTPS with a plain account password. GitHub retired that path
for Git operations years ago — it needs a token or an SSH key. Dead on arrival.
</ProcessStep>

<ProcessStep result="fail" title="Registered the SSH key as a read-only deploy key" tag="my-setup">
Switching to SSH, I added the key as a repo **deploy key**. Those default to
read-only, so the handshake succeeded and the push was still denied. Recreating
the repo didn't help — the fix was never the repo, it was the key's scope.
</ProcessStep>

<ProcessStep result="fail" title="Wrong Git identity, and two keys in the agent" tag="my-setup">
The first commit landed under a leftover global `user.email` from another
project. On top of that, `ssh-add -l` showed the old and new keys both loaded, so
which one GitHub actually accepted was ambiguous — it kept authenticating through
the stale, read-only entry.
</ProcessStep>

<ProcessStep result="pass" title="What worked">
A repo-local Git identity (`git config --local`), a single account-level SSH key
(not a deploy key, so there's no read/write toggle to fight), and clearing the
agent down to just that one key. Clean first commit, correct author, full
scaffold pushed. Boring, but honest.
</ProcessStep>

**How this was actually made** — worth a note, because it's a technique you can
steal. The concept and the page designs didn't come from one AI in one pass. Most
of the thinking was done in Claude (Opus 4.8), with ChatGPT (5.6) pulled in as a
second opinion; the UI mockups were made by Claude, then critiqued by ChatGPT, with
the useful feedback folded back in by Claude. Having one model red-team another's
work caught things the first missed. Simple move, real payoff — the full
back-and-forth, with the actual critiques and prompts, is in the repro pack.

## The test

For a site build the check isn't a question set — it's whether the foundation
actually holds. What's verified so far:

<StatTiles variant="metric" stats={[
  { value: "1", label: "collection drives the whole site", tone: "ok" },
  { value: "4", label: "views from that one source", tone: "ok" },
  { value: "0", label: "page edits to add a build", tone: "ok" },
]} />

Adding a markdown file lights up the home page, the archive, its own detail page,
and the tools index with no page-code changes — the core invariant passes. The
"Runs on this site" badge and the freshness states render straight from
frontmatter. And it's shipped: deployed on Netlify and live at
[jigg.ai](https://jigg.ai), on a custom domain with HTTPS. The one check still
logged as pending rather than claimed is a Lighthouse performance pass.

<StatTiles variant="fact" stats={[
  { value: "$0", label: "Hosting (static, free tier)" },
  { value: "1 file", label: "To publish a new build" },
  { value: "Astro 7.1", label: "Framework version" },
]} />

## Reproduce this

<ReproPack
  onPage={[
    "The simplified content schema",
    "One complete example record",
    "An AI workflow overview",
    "The one-collection-to-four-views diagram",
    "An annotated repository-tree excerpt",
    "Several representative prompts — including one failed prompt and its correction",
  ]}
  packDescription="Reproduce this build — Get the complete content schema, AI workflow, architecture diagram, curated build prompts, annotated repository structure, and runnable Astro scaffold used to build Jigg.AI."
  packContents={[
    "Complete schema and sample dataset",
    "Full-resolution editable diagrams",
    "Complete curated prompt sequence",
    "Full repository map",
    "Runnable Astro scaffold",
    "Setup guide",
    "Deployment checklist",
    "Reusable templates",
  ]}
/>

## The tool

<ToolVerdict tool="Claude Code" toolSummary="AI pair-builder in your terminal">
Claude Code is genuinely good at this kind of greenfield scaffold: it held the
whole four-views-from-one-collection idea in its head and wrote the schema, the
templates, and a reusable component kit to match. Astro's content collections are
the right backbone for it. Neither tool caused the friction here — that was all
local Git config. If you can get past the auth setup once, the build loop is fast.
</ToolVerdict>
